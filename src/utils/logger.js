/**
 * Log padrão
 * @param {string} msg
 * @return {void}
 */
export function log(msg) {
	console.log(`\n ${msg}`);
}

/**
 * Log de erro
 * @param {string} msg
 * @return {void}
 */
export function error(msg) {
	console.error(`\n ${msg}`);
}
